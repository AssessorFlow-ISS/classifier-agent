"""Vendored minimal af-shared shim for CI smoke runs.

Subset of upstream ``assessorflow/shared`` containing only the symbols the
classifier actually imports. Replace with ``pip install git+https://...`` once
a ``SHARED_REPO_PAT`` repo secret is provisioned.

Vendored at: 2026-05-03
Upstream: assessorflow/shared @ main
"""
