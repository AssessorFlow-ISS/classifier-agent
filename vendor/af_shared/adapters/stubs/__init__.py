"""In-memory stub adapters used by the af_shared factory functions.

Both adapters record every call to a list attribute so tests can assert
on the captured data without spinning up real backends.
"""
